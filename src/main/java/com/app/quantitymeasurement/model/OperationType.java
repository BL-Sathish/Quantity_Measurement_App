package com.app.quantitymeasurement.model;

/**
 * Enum representing the types of operations performed by the application.
 */
public enum OperationType {
    ADD,
    SUBTRACT,
    MULTIPLY,
    DIVIDE,
    COMPARE,
    CONVERT
}
